import { Presentation } from '@/app/icons'
import Encabezado from '@/components/Encabezado'

export default function EncabezadoPresentar() {
  return <Encabezado titulo="Presentar" icon={<Presentation size={40} />} />
}
