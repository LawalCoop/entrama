import { ClipboardList } from '@/app/icons'
import Encabezado from '@/components/Encabezado'

export default function EncabezadoRecolectar() {
  return <Encabezado titulo="Recolectar" icon={<ClipboardList size={40} />} />
}
