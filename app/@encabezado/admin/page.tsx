import { ClipboardList } from '@/app/icons'
import Encabezado from '@/components/Encabezado'

export default function EncabezadoAdmin() {
  return <Encabezado titulo="Problemas" icon={<ClipboardList size={40} />} />
}
